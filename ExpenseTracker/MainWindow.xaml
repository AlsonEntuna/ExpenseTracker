<Window
    x:Class="ExpenseTracker.MainWindow"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:local="clr-namespace:ExpenseTracker"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    xmlns:view="clr-namespace:ExpenseTracker.View"
    Title="Expense Tracker"
    Width="1720"
    Height="1000"
    Background="{StaticResource Brush.Main.Background}"
    Closing="Window_Closing"
    MouseDown="Window_MouseDown"
    ResizeMode="NoResize"
    WindowStyle="None"
    mc:Ignorable="d">
    <Window.DataContext>
        <local:MainWindowViewModel />
    </Window.DataContext>
    <Grid>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="80" />
            <ColumnDefinition Width="*" />
        </Grid.ColumnDefinitions>
        <Grid Grid.Column="0">
            <Border Panel.ZIndex="-1" Background="{StaticResource Brush.Main.Background.Alt}" />
            <Grid Grid.Column="0" Panel.ZIndex="0">
                <Grid.RowDefinitions>
                    <RowDefinition Height="*" />
                    <RowDefinition Height="*" />
                    <RowDefinition Height="*" />
                    <RowDefinition Height="*" />
                    <RowDefinition Height="*" />
                    <RowDefinition Height="*" />
                    <RowDefinition Height="*" />
                    <RowDefinition Height="*" />
                    <RowDefinition Height="*" />
                    <RowDefinition Height="*" />
                    <RowDefinition Height="*" />
                    <RowDefinition Height="*" />
                    <RowDefinition Height="*" />
                </Grid.RowDefinitions>
                <Button
                    x:Name="Btn_Open"
                    Grid.Row="1"
                    Width="70"
                    Height="70"
                    Click="Btn_Open_Click"
                    Style="{StaticResource ButtonStyle.Main}">
                    <Button.ContentTemplate>
                        <DataTemplate>
                            <ContentPresenter Content="{StaticResource OpenIcon}" />
                        </DataTemplate>
                    </Button.ContentTemplate>
                </Button>

                <Button
                    x:Name="Btn_New"
                    Grid.Row="2"
                    Width="70"
                    Height="70"
                    Click="Btn_New_Click"
                    Style="{StaticResource ButtonStyle.Main}">
                    <Button.ContentTemplate>
                        <DataTemplate>
                            <ContentPresenter Content="{StaticResource NewIcon}" />
                        </DataTemplate>
                    </Button.ContentTemplate>
                </Button>

                <Button
                    x:Name="Btn_Exit"
                    Grid.Row="15"
                    Width="70"
                    Height="70"
                    Click="Btn_Exit_Click"
                    Style="{StaticResource ButtonStyle.Main}">
                    <Button.ContentTemplate>
                        <DataTemplate>
                            <ContentPresenter Content="{StaticResource ExitIcon}" />
                        </DataTemplate>
                    </Button.ContentTemplate>
                </Button>
            </Grid>
        </Grid>
        <StackPanel Grid.Column="1" Margin="20">
            <TextBlock Style="{StaticResource TextBlockStyle.Title}" Text="Expense Tracker" />
            <view:VariableExpenseView DataContext="{Binding VariableExpenseViewModel}" />
        </StackPanel>
    </Grid>
</Window>
