<Window
    x:Class="ExpenseTracker.MainWindow"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:local="clr-namespace:ExpenseTracker"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    xmlns:view="clr-namespace:ExpenseTracker.View"
    Title="Expense Tracker"
    Width="800"
    Height="800"
    Background="{StaticResource Brush.Main.Background}"
    Closing="Window_Closing"
    SizeToContent="Height"
    WindowStyle="ThreeDBorderWindow"
    mc:Ignorable="d">
    <Window.DataContext>
        <local:MainWindowViewModel />
    </Window.DataContext>
    <Grid>
        <StackPanel>
            <Grid Background="{StaticResource Brush.Main.Header_Alt}">
                <TextBlock Style="{StaticResource TextBlockStyle.Header1}" Text="Expense Tracker" />
            </Grid>
            <Border
                Width="500"
                Height="5"
                Margin="3"
                HorizontalAlignment="Left"
                Background="{StaticResource Brush.Main.Text}" />
            <Border
                Width="600"
                Height="5"
                Margin="3"
                HorizontalAlignment="Left"
                Background="{StaticResource Brush.Main.Text}" />
            <Border
                Width="700"
                Height="5"
                Margin="3"
                HorizontalAlignment="Left"
                Background="{StaticResource Brush.Main.Text}" />
            <Grid>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*" />
                    <ColumnDefinition Width="*" />
                </Grid.ColumnDefinitions>
                <Button
                    Grid.Column="0"
                    Margin="5"
                    Command="{Binding OpenVariableExpenseCommand}"
                    Content="Open Budget"
                    Style="{StaticResource ButtonStyle.Main}" />
                <Button
                    Grid.Column="1"
                    Margin="5"
                    Command="{Binding CreateVariableExpenseCommand}"
                    Content="Create Variable Expense"
                    Style="{StaticResource ButtonStyle.Main}" />
            </Grid>
            <view:VariableExpenseView DataContext="{Binding VariableExpenseViewModel}" />
        </StackPanel>
    </Grid>
</Window>
