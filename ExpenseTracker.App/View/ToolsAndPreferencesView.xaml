<UserControl
    x:Class="ExpenseTracker.View.ToolsAndPreferencesView"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:local="clr-namespace:ExpenseTracker.View"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    xmlns:tools="clr-namespace:ExpenseTracker.View.Tools"
    xmlns:viewmodel="clr-namespace:ExpenseTracker.ViewModels"
    xmlns:views="clr-namespace:ExpenseTracker.View"
    d:DataContext="{d:DesignInstance viewmodel:ToolsAndPreferencesViewModel,
                                     IsDesignTimeCreatable=True}"
    d:DesignHeight="450"
    d:DesignWidth="800"
    mc:Ignorable="d">
    <Grid>
        <ScrollViewer>
            <StackPanel>
                <Expander Header="Expense">
                    <tools:CategoriesEditorView DataContext="{Binding CategoriesEditorViewModel}"/>
                </Expander>
            </StackPanel>
        </ScrollViewer>
    </Grid>
</UserControl>
