<UserControl
    x:Class="ExpenseTracker.View.Tools.ExpenseCategoryControl"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:controls="clr-namespace:ExpenseTracker.Wpf.Controls;assembly=ExpenseTracker.Wpf"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:local="clr-namespace:ExpenseTracker.View.Tools"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    x:Name="RootControl"
    MinHeight="200"
    d:DataContext="{d:DesignInstance local:ExpenseCategoriesViewModel,
                                     IsDesignTimeCreatable=False}"
    d:DesignHeight="450"
    d:DesignWidth="800"
    mc:Ignorable="d">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="45" />
            <RowDefinition Height="*" />
        </Grid.RowDefinitions>
        <!--  Title  -->
        <TextBlock
            Grid.Row="0"
            Style="{StaticResource TextBlockStyle.Header1.Bold}"
            Text="Expense Categories" />
        <ScrollViewer
            Grid.Row="1"
            HorizontalScrollBarVisibility="Auto"
            VerticalScrollBarVisibility="Auto">
            <ListView
                MinHeight="{Binding ActualHeight, ElementName=RootControl, Converter={StaticResource SubtractConverter}, ConverterParameter=500}"
                Background="#2CCC"
                BorderBrush="Transparent"
                ItemsSource="{Binding ExpenseCategories}"
                SelectedItem="{Binding SelectedExpenseCategory, UpdateSourceTrigger=PropertyChanged, Mode=TwoWay}">
                <ListView.ContextMenu>
                    <ContextMenu>
                        <MenuItem Command="{Binding AddExpenseCategoryCommand}" Header="Add" />
                        <MenuItem Command="{Binding RemoveExpenseCategoryCommand}" Header="Remove" />
                    </ContextMenu>
                </ListView.ContextMenu>
                <ListView.ItemTemplate>
                    <DataTemplate>
                        <controls:EditableTextBlock TextSource="{Binding CategoryName}" TriggerObject="{Binding Editing}" />
                    </DataTemplate>
                </ListView.ItemTemplate>
            </ListView>
        </ScrollViewer>
    </Grid>
</UserControl>